<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update todo</title>
    <link rel="stylesheet" href="./css/update.css">

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap');

*{
   padding: 0;
   margin: 0;
   box-sizing: border-box;
   font-family: 'Poppins', sans-serif;

   }
   .container{
       width: 100vw;
       height: 100vh;
       display: flex;
       flex-direction: column;

   }

   .section-heading{
       padding: 20px 0;
       font-size: 1.7rem;
       text-align: center;
   }
   .label{
       display: block;
       font-size: 1.2rem;
       font-weight: 500;
       margin: 5px;
   }
   form{
       width: 50%;
       margin: 10% auto;
       text-align: center;
       border: 2px solid gray;
       border-radius: 5px;
   }
   .input-group input{
       width: 80%;
       padding: 10px 20px;
       font-size: 17px;
       border: 2px solid transparent;
       border-radius: 5px;
       box-shadow: 1px 1px 10px rgba(0, 0, 0, 0.219);
       outline: none;
   }
   .input-group input:focus{
       border: 2px solid lightblue;
   }
   .btn-container{
       width: 100%;
   }
   .btn-container button{
       padding: 5px 20px;
       margin: 30px 0;
       font-size: 18px;
       background-color: rgb(65, 33, 243);
       color:white;
       border-radius: 4px;
       border: none;
       letter-spacing: 1px;
       cursor: pointer;
       box-shadow: 1px 1px 10px rgba(0, 0, 0, 0.514);
   }
    </style>
</head>
<body>
    <div class="container">
        <form action="/update_item/<%= data.id %>" method="POST">
            <h2 class="section-heading">Update Todo</h2>
            <div class="input-group">
                <!-- <label for="todo" class="label">Todo</label> -->
                <input type="text" name="task" value="<%= data.task %>" placeholder="Update your task">
            </div>
            <div class="btn-container">
                <button type="submit">Update</button>
            </div>
        </form>
    </div>
    
</body>
</html>